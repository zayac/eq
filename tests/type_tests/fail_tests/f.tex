\begin{eqcode}{\mu}{\ }{\ }{\type{Z}}
  a \in \type{Z} \lend
  a^{[0]} \gets 0 \lend
  a^{[1]} \gets 1 \lend
  a^{[\iter]} \gets a^{[\iter-1]} \lend
  \return {a^{[5]}} \lend
\end{eqcode}

% eq-error: too many initial values found for variable `a'
