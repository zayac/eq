\begin{eqcode}{\mu}{\ }{\ }{\arraytype{\ldots,\ldots}{Z}}
  s \gets \begin {tmatrix}
  100 \lend
  100 \lend
  \end{tmatrix} \lend
  a \gets \genar \limits ^{\begin{tmatrix} 2 \lend 2 \lend \end{tmatrix}} (0) \lend
  a_{i,j} | i,j : i < 5 \cap j < 5 \gets 5 \lend
  a_{i,j} | i,j : i \geq 5 \cap j < 10 \gets
  \begin{cases}
    i \cdot j & j: j < 5 \lend
    a_{i-1, j-1} & \otherwise \lend
  \end{cases} \lend
  a_{a,1} \gets 42 \lend % eq-error-loc: the index must be `\type {Z}'
  \return {a} \lend
\end{eqcode}
