\begin{eqcode}{\mu}{\ }{\ }{\type{Z}}
  a \in \type{Z} \lend
  a^{[0]} \gets 0 \lend
  a^{[1]} \gets 1 \lend
  a^{[\iter]} \gets a^{[\iter-2]} + a^{[\iter-3]} \lend
  \return {a^{[5]}} \lend
\end{eqcode}

% eq-error: there have to be 3 consequent initial values for variable `a'
