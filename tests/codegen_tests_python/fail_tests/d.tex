\begin{eqcode}{\mu}{\ }{\ }{\type{Z}}
  a \in \overline{\type{Z}} \lend
  b \gets 2 \lend
  c \in \overline{\type{Z}} \lend
  c^{[\iter]}, a^{[\iter]} \gets 4, \iter \cdot \iter \cdot b \lend  % eq-error-loc: it is forbidden to define several different recurrent expressions in one statement. `c' and `a' variables conflict occured
  \print {c^{[10]}, a^{[5]}} \lend
\end{eqcode}
